version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18

  pre_build:
    commands:
      - cd client
      - ls
      - npm install

  build_client:
    commands:
      - cd client
      - ls
      - npm run build

  build_server:
    commands:
      - cd ../server
      - ls
      - npm install

artifacts:
  files:
    - client/build/**/*
